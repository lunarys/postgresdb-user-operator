---
# Minimal CNPG Cluster CRD stub for envtest.
# This file is only used in tests so that the controller can List
# postgresql.cnpg.io/Cluster resources. It does not define the full
# CNPG schema — x-kubernetes-preserve-unknown-fields allows creating
# Cluster objects with arbitrary structure from test code.
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  name: clusters.postgresql.cnpg.io
spec:
  group: postgresql.cnpg.io
  names:
    kind: Cluster
    listKind: ClusterList
    plural: clusters
    singular: cluster
  scope: Namespaced
  versions:
  - name: v1
    served: true
    storage: true
    schema:
      openAPIV3Schema:
        type: object
        x-kubernetes-preserve-unknown-fields: true
